# Workspace Rules

CRITICAL RULE: The directory `C:\Users\opole\Downloads\ChatBotn` is a strict READ-ONLY reference project. You are FORBIDDEN from executing any file edits, modifications, or write commands targeting this directory while operating in this workspace. All code generation and modifications must be strictly confined to the active workspace (`ArchVantage`).
