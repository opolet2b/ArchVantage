# Documentation Rule

To maintain a clean project root, all documentation files should be placed in the `docs` directory.

## Rule
- Always place new documentation files (markdown, guides, manuals, etc.) in the `docs` directory at the project root.
- The only exception is the main `README.md` file, which should remain in the root directory.
- When creating or updating documentation, ensure that links between documents correctly reflect the directory structure.
