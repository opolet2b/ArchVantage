"use client";

import { SpacesList } from "@/components/spaces/spaces-list";

export default function SpacesPage() {
    return (
        <div className="h-full w-full bg-slate-50 min-h-screen">
            <SpacesList />
        </div>
    );
}
