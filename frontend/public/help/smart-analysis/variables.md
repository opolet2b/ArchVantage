# No-Code Variables
Define variables to make this template reusable and dynamic.
- **Usage**: Variables appear as input fields when a user runs this template.
- **Types**: Text, Number, Date, Select (Dropdown).
- **Example**: Create a `Company Name` variable so the user can specify which company to analyze each time they run the template.
