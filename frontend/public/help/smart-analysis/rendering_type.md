# Rendering Type
The specific visual format for the output.
- **Depends on Category**: Options are determined by the selected "Synthesis Category".
- **Visualizer**: Controls how the data is presented in the UI (e.g., Mermaid Graph, Bar Chart, Simple Text).
