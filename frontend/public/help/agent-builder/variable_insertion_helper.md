# Variable Insertion Helper

Use this dropdown to quickly insert variables into your instruction text.

- **Populated from**: The keys you have defined in the **Input Schema** section.
- **How to use**: Select a variable from the list and click **Insert**.
- **Syntax**: The variable will be inserted at your cursor position wrapped in `{{ }}` curly braces, which allows the LLM engine to resolve the actual value at runtime.
