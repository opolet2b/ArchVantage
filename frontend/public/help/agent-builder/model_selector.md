# Model Selection

Choose the Large Language Model (LLM) that will power this agent.

- **Local Models**: Run on your own machine (e.g., via Ollama). Good for privacy and cost.
- **Remote Models**: Cloud-based models (e.g., GPT-4, Claude). Generally more capable but cost money per request.
