# Iterate Over (Source Items)

Specify the list of items you want to loop through. 

- This should be a reference to a variable containing a list/array (e.g., `{{previous_node.results}}`).
- The loop will execute its internal steps once for each item in this list.
- Use the **Variable Picker** icon to select a field from a preceding node.
